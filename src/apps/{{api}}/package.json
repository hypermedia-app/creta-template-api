{
  "name": "api",
  "version": "0.0.0",
  "private": "true",
  "scripts": {
    "bootstrap": "talos put --api $api_BASE --endpoint $SPARQL_ENDPOINT --user $SPARQL_USER --password $SPARQL_PASSWORD",
    "build": "tsc"
  },
  "dependencies": {
    "@hydrofoil/knossos": "^0.9.3"
  },
  "devDependencies": {
    "@hydrofoil/talos": "^0.4.4"
  }
}
